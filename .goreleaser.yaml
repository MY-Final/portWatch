version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: portwatch
    main: ./cmd/portwatch
    binary: portwatch
    ldflags:
      - -s -w -X main.version={{ .Version }}
    goos: [windows, linux, darwin]
    goarch: [amd64, arm64]

archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt
