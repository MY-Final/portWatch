# 901 CI Matrix

目标：新增 `.github/workflows/ci.yml`，在 Windows/Linux/macOS 运行 `go test ./...`、`go vet ./...`，并在 Windows 验证构建。

负责文件：`.github/workflows/ci.yml`。依赖：Phase 7/8 的实现任务。验收：workflow 使用固定 Go 版本、缓存依赖、失败时上传测试日志；不改 Go 源码。

