# Backlog 001 UDP

## 目标
为 UDP socket 增加独立 `Protocol` 扫描接口和 Windows/Linux/macOS 实现。

## 文件边界
预期文件为 `pkg/model/port.go` 的协议扩展、各平台 `internal/port/*` UDP 文件和对应测试；不得改变 TCP 默认行为。

## 依赖
Phase 7/8 完成，并先完成模型和 JSON schema 评审。

## 验收与测试
固定 UDP fixture 能解析，`--protocol tcp` 的结果与当前版本完全一致；各平台 `go test ./...` 通过。
